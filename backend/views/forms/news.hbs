<div class="row">
    {{#if msg}}<p>{{msg}}</p>{{/if}}
<div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3>{{title}}</h3>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-12">
                        
                        <p>Ce formulaire vous permet de créer de nouvelles news pour tenir vos utilisateurs au courant des dernières informations....</p>
                        

                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12">
                        

                        
                        <form role="form" method="POST" action="/admin/news">
                           
                            <div class="form-group">
                                <label>Titre de la News</label>
                                <input class="form-control" name="title" required value="{{product.nom}}">
                                <p class="help-block">Le titre est la premiere chose qu'on lit,....</p>
                            </div>
                            <div class="form-group">
                                <label>Contenu</label>
                                <textarea class="form-control" rows="3" name="texte">{{product.description}}</textarea>
                                
                            </div>
                            

                             <div class="form-group">
                                <label>File input</label>
                                <input type="file">
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn btn-primary btn-lg">Enregistrer</button>
                                <button type="reset" class="btn btn-default btn-lg">Abandonner</button>
                                
                            </div>
                        </form>
                    </div>
                    
                </div>
                <!-- /.row (nested) -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
</div>